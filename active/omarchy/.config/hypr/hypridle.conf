general {
    lock_cmd = omarchy-lock-screen                         # lock screen and 1password
    before_sleep_cmd = loginctl lock-session               # lock before suspend/hibernate
    after_sleep_cmd = sleep 1 && hyprctl dispatch dpms on  # delay for PAM readiness, then turn on display after wake
    inhibit_sleep = 3                                      # wait until screen is locked before sleep/hibernate
}

# listener {
#     timeout = 150                                             # 2.5min
#     on-timeout = pidof hyprlock || omarchy-launch-screensaver # start screensaver (if we haven't locked already)
# }

listener {
    timeout = 900                      # 15min
    on-timeout = omarchy-lock-screen   # use Omarchy's lock flow instead of generic session lock
}

# listener {
#     timeout = 151                      # 5min
#     on-timeout = loginctl lock-session # old lock timing
# }

# listener {
#     timeout = 330                                            # 5.5min
#     on-timeout = brightnessctl -sd '*::kbd_backlight' set 0  # save state and turn off keyboard backlight
#     on-resume = brightnessctl -rd '*::kbd_backlight'         # restore keyboard backlight
# }

# listener {
#     timeout = 330                                            # 5.5min
#     on-timeout = hyprctl dispatch dpms off                   # screen off when timeout has passed
#     on-resume = hyprctl dispatch dpms on && brightnessctl -r # screen on when activity is detected
# }

listener {
    timeout = 2700                    # 45min
    on-timeout = systemctl hibernate  # hibernate when timeout has passed
}
